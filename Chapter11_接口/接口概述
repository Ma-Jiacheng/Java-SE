接口概念引入：
    1.生活中常见的电源插口就是一个接口，接口由国家制定统一标准，不同的用电器都可以使用相同的电源接口。
      接口就是一种公共统一的规范标准，只要符合规范标准就可以通用。
    2.Mac系统电脑、Windows系统电脑、打印机、U盘等不同类型、不同系统的设备之间可以相互进行数据传输，
      不是因为在开发之前各公司进行协商预先处理，而是在制造时都遵循 Inter 公司提出的 USB 公共规范保准。

在 Java 9 版本之后，接口的内容可以有：
    1.成员变量（其实是常量）：
        格式：【public】 【static】 【final】 数据类型 常量名称 = 数据值；
        注意：
            1.常量必须进行赋值，赋值之后不能改变
            2.常量名称完全大写，用下划线进行分割
            3.三个关键字书写时可以省略，但时程序中仍然存在

    2.接口中最重要的就是抽象方法
        格式：【public】 【abstract】 返回值类型 方法名称（参数列表）；
        注意：实现类必须重写接口所有抽象方法，除非实现类是抽象类

    3.从 Java 8 开始，接口里允许定义默认方法：
        格式：【public】 default 返回值类型 方法名称（参数列表）{方法体}
        注意：默认方法也可以被覆盖重写

    4.从 Java 8 开始，接口里允许定义静态方法
        格式：【public】 static 返回值类型 方法名称（参数列表）{方法体}
        注意：应通过接口名称进行调用，不能通过实现类对象调用接口静态方法

    5.从 Java 9 开始，接口里允许定义私有方法
        格式：
            普通私有方法：private 返回值类型 方法名称（参数列表）{方法体}
            静态私有方法：private static 返回值类型 方法名称（参数列表）{方法体}
        注意：private的方法只有接口自己才能使用，不能被实现类使用